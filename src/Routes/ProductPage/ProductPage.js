import React, { Component } from 'react';

export default class ProductPage extends Component {
    static defaultProps = {
        match: { params: {} },
    }

    render() {
        return (
            <></>
        );
    }
}